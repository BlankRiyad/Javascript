/* Backtic characters are alternative way to write 
strings without using double qoute. We write string variable after the $ sign followed 
by curly braces */

let old = `Superman`;
let latest = `Startek`;
let movies = `${old}, ${latest}`;

console.log(movies);
